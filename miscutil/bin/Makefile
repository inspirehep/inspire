# $Id$

include ../../config.mk
-include ../../config-local.mk

BINFILES = retryupload

BINDIR = $(PREFIX)/bin

all:
	@echo "Done."

test:
	@echo "Nothing to test."

install:
	$(INSTALL) -m 755 $(BINFILES) $(BINDIR)
