# $Id$

include ../config.mk

CFGFILES = citation.cfg wrd.cfg
CFGDIR = $(ETCDIR)/bibrank

all:
	@echo "Done."

test:
	@echo "Not implemented."

install:
	$(INSTALL) -m 664 $(CFGFILES) $(CFGDIR)

clean:
	@rm -f *.orig *~
